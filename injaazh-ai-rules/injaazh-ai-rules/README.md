# Injaazh AI Rules

Place the `.ai/rules` folder in your project root.

When prompting Antigravity IDE, instruct it:

'Before implementing any feature, read every file inside `.ai/rules` and
strictly follow those rules throughout planning, coding, refactoring, UI
design and responsiveness.'
